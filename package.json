{
  "name": "vsc",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "cli": "bun run src/index.ts",
    "cli:test": "bun run src/test.ts",
    "compile": "bun build ./src/index.ts --compile --outfile index && chmod +x index"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/inquirer": "^9.0.7"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^4.3.0",
    "chalk": "^5.3.0",
    "inquirer": "^9.2.16"
  }
}