{
  "name": "vsc",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "cli": "bun run src/cli.ts",
    "cli:test": "bun run src/test.ts",
    "compile": "bun build ./src/cli.ts --compile --outfile index && chmod +x index"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/inquirer": "^9.0.7"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^4.3.0",
    "inquirer": "^9.2.16"
  }
}